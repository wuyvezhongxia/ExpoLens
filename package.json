{
  "name": "expolens",
  "version": "0.3.0",
  "private": true,
  "description": "Zero-config Response Preview for any Expo/RN project. Listens to Metro /inspector/network; never modifies your app.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
