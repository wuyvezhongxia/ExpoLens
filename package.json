{
  "name": "expolens",
  "version": "0.3.1",
  "private": true,
  "description": "Zero project-file-change Response Preview for any Expo/RN app via NODE_OPTIONS preload.",
  "type": "module",
  "bin": {
    "with-expolens": "./bin/with-expolens.mjs"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js",
    "with": "node ./bin/with-expolens.mjs"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
